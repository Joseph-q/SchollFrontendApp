interface QueryParamsAssistanceStudentById {
    courseId: number; // Equivalente a uint en Go
    orderBy: string;
    date?: string; // Propiedad opcional
    page: number;
    limit: number;
  }
  